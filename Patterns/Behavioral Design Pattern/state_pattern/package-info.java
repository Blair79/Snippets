/**
In State pattern a class behavior is changed based on its state.
State pattern is a behavior pattern.
When using State pattern, we create various state objects and a context object whose behavior
varies as its state object changes.
 */
/**
 * @author ColBl
 *
 */
package state_pattern;